eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/
    register-with-eureka: false
  #设置自我保护中心
  server:
    enable-self-preservation: false
spring:
  application:
    name: eureka
management:
  context-path: /
server:
  port: 8761